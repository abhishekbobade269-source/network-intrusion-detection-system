# Loaded automatically by `vite build --mode demo` (npm run build:demo).
# Switches the whole app onto the in-memory fixture store in
# src/demoStore.ts / demoFixtures.ts — no backend, no websocket, no API
# key required. See dashboard/src/demoStore.ts for what this replaces.
VITE_DEMO_MODE=true
